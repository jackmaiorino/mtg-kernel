# Native history-value search live v3 result

The rapid fresh matched gate completed eight mutually successful pairs at base
seed `1240001` in 92 seconds with no mapper exclusions.

The fixed one-step information-set search scored 7 wins in 16 games. The
retained parent scored 8. At the game level, search gained 2 outcomes, lost 3,
and tied 11 relative to the parent. Search-minus-parent win count was 0 at P0
and -1 at P1. The required `G >= L + 2` gate failed.

The selector was active and mechanically valid. It evaluated 485 eligible
roots, made 84 overrides with at least one in each seat, and had zero
four-sample distinctness violations. The negative result therefore rejects
this fixed one-step value rule, not learned value, belief modeling, or deeper
search in general.

The base-`1250001` extension is unauthorized. Do not tune the override margin,
root filter, or information-set sample count against this report.
