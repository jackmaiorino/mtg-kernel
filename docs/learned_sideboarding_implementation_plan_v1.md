# Learned sideboarding integration, September 12, 2026

Owner: Codex, explicitly assigned by Jack. Base1ebb3b6b preserves completed card wave1 and the harness, excluding unfinished Urzatron. Worktree E:/mtg-kernel-learned-sideboarding-codex. Fable's card tree and the active science lane remain independently owned.

Deliver a real frozen checkpoint playing cross-archetype BO3 with a learned legal sideboard policy. Begin with supported Rally, Affinity and Terror, then Elves; this is engineering coverage, not an outcome-selected scientific panel. Include both seats and some mirrors. Brewing remains a destination through card-set inputs, configuration identities and arbitrary-deck episode construction.

1. Repair the paired harness: actor-visible neural inputs, per-seat sampling reset, natural terminals, improvement-only BO3 acceptance, and truthful exposure status. Verify harmful candidates cannot be accepted and identical paired arms reproduce actions.
2. Implement a small CPU learned sideboard head over immutable play-model card embeddings, pooled own75/opponent evidence and match context. Use legal out/in/Done actions and supervised targets with optional measured values. Test actual conditional learning, permutation invariance, serialization, frozen weights and legal completion.
3. Connect live per-seat configurations to BO3 without changing existing Store admission. Validate original75 conservation transactionally, game1 registration, subsequent play/draw chooser, and complete natural outcomes. Project only the acting player's private card outcomes and legitimately observed opposing identities.
4. Provide CLI entrypoints for pinned play import, complete cross-deck matches, source-labelled imitation examples, training and persisted-policy replay. Generate genuine game contexts with a real imported player. A fact-checked static table is a disclosed warm start, not a certified search result or evidence that a learned policy wins more.
5. Qualify the first working integration, then prepare bounded search targets and post-board play exposure. Keep the player fixed within teacher generation and comparison. Formal acceptance compares no swaps, static tables and learned swaps on matched BO3 seeds, with reserved evaluation configurations/policies and integer win gates settled before measurement. Current gate constants remain unchanged.

Validation stays focused on these seams. Builds and scratch data stay on E:. One small execution manifest records commit, toolchain/linker, seeds and input/output hashes. No new GPU campaign, rented compute or main-tree merge is implied by engineering completion. Main-tree integration happens at the existing campaign boundary. Request independent Fable cross-examination of implementation and measurement decisions; a quota failure is recorded explicitly and never treated as endorsement.
