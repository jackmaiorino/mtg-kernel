# Prospective BO3 opening correction

Source117b2c22839a4406b3b135bb192dcf22dd92e984 adds explicit keep_seven_v2 to the BO3 run configuration. Both players keep seven using the existing human opening transition, clearing setup draw counters and setup events before normal play. The legacy default retains its constructors and exact serialized output bytes. Corrected mode requires V3 and is recorded in every physical game's containing match configuration. The running and archived training binaries were not replaced by this change.

Eight library checks and14CLI checks passed. They cover exact legacy serialization, both starters and game2, human Keep7 state/action equality, first-turn draw counters and natural next-player draws, unsupported/unknown mode rejection, and existing population/config behavior. A release evaluator and real full-match replay remain outstanding. These are engineering checks, not playing-strength results.

The upcoming fixed-final-player comparison uses the corrected protocol in both arms. Its exact original75s and originalAdam3 development anchor remain. The opening change is a known transfer from the legacy training protocol; it does not establish equivalence to human mulligans or adaptive play/draw choice. Candidate selection is fixed at480updates, without consulting training wins or CP7 outcomes.

Offline execution binder/serial runner and paired analyzer are prepared under `E:/mtg-kernel-learned-sideboarding-evidence/bo3-post480-preparation-001`. The proposed comparison has3,200paired BO3 cases; it remains draft until the final checkpoint, common evaluator and outcome-blind cost calibration are bound. The analyzer's21synthetic checks do not measure player strength. Independent source/design reviews completed. Fresh Fable consultations failed weeklyHTTP429 with zero reads or substantive feedback; this is recorded in `opening-keep7-v2/DECISION.md` and is not endorsement.

Training subsequently stopped after243completed updates in continuation004, with a separate actor-visible encoding failure on original243. That repair now has priority; see `overnight-development-001/failure-iteration243-001/STATUS.md`. All previous evidence and the full campaign goal remain preserved.
