# V3 numerical evidence and human decision adapter

This continues the full Multi-Deck BO3 Campaign from documentation commit `ecb43c5377121367a3d6cfd28df858b5e5a45046`. Engineering-008 source `20ebd711` and its CPU/CUDA binaries remain immutable. The new work adds diagnostic access to actual device arithmetic and a human decision boundary. It does not complete GPU qualification, human BO3, broader learning or brewing.

## Numerical evidence

An explicitly selected test-only V3 API returns logits and values from the exact CUDA forward used for backward, each actual loss scalar consumed by backward, their group/chunk mapping, and all 33 gradients before Adam. The aggregate is explicitly an f64 host sum of device chunk scalars. Production and existing default/legacy diagnostics keep their original readbacks. Capture validation precedes Adam and final host replacement; failed capture cannot publish a host update. Existing bridge receipt loss still means transported CPU evidence.

Two ignored real-trajectory diagnostics accept a pinned original update command, verify both actors' exact behavior and isolate learner groups. CPU preparation exports raw tensor/action/output bits, the common initial state, all CPU gradients and the resulting full state. The separately selected CUDA diagnostic adds actual device evidence and full CUDA state. Output directories must be new; partial CPU references after a later device failure have no completion receipt. Root executed the CPU-only plan at committed source `c906984b6755ddddc538133aba8e9a82937b8ec5`; the test took 13.18 seconds, including a 1.205-second CPU update. Initial and final parameters, both moment streams, gauge anchor, Adam step and full-state identities exactly match the frozen engineering-006 and engineering-007 references. All 33 gradient names/shapes and output hashes verified. See [CPU verification](E:/mtg-kernel-learned-sideboarding-evidence/engineering-009/cpu-reference-verification.json) and [sealed test-binary manifest](E:/mtg-kernel-learned-sideboarding-evidence/engineering-009/cpu-reference-run-001/manifest.json). Original inputs and outputs remain unchanged. Device execution remains unperformed.

`python/tools/compare_v3_numerical_probe_v1.py` reads a completed actual-device diagnostic and its hashed outputs. It reports forward, selected probability/log-probability, objective, all-gradient, parameter, parameter-delta and moment differences. It verifies common origin, group/chunk/loss identity and named tensor correspondence. Short serde f32 decimals are restored to their binary32 values before comparison. RMS floors are explicit report settings, not acceptance thresholds. Five focused Python tests passed, including a missed small update, gauge-invariant probabilities, and mismatched loss provenance. The report never declares numerical qualification or speedup.

Root independently reviewed the CUDA capture source. Its author then reviewed the separate real-trajectory exporter and comparison tool. Accepted findings: explicitly type the CPU-only optional timing value; bind gradients/Adam and common origin; verify chunk objective/configuration; preserve f32 decimal round trips; retain group-to-episode provenance. Actual device qualification still needs a declared envelope, real Ward and broader learner-deck coverage, same-backend repeat/continuation, and useful end-to-end throughput. The real reference batch alone does not supply every required case.

## Human decision boundary

The new in-process adapter fixes one human seat, obtains the V6 observation and exact legal semantics from the same validated V3 binding, and keeps its execution map private. Explicit visible DTOs replace arena/incarnation identifiers with local handles. Human action ordering maps back to exact engine actions; repeated requests return a bounded cached receipt and stale requests reject. One unsupported description rejects the whole prompt rather than hiding a legal choice.

Independent review required nonmonotonic allocation tests, safe duplicate hand copies, removal of artificial equipment timestamp order, and Ward wording that identifies the spell/ability declined payment would counter. Default library compilation passed in 20.42 seconds and all 16 focused human tests passed. Two initial equipment fixtures lacked a legal human choice and auto-passed to the opponent; root added an own-hand Forest to preserve the intended human prompt, after which both permutation tests passed. CUDA production compilation passed in 41.76 seconds. Its complete test harness compiled in 66 seconds and six selected host-only CUDA tests passed; the device diagnostic stayed ignored. Alongside five Python checks, this is 27 focused checks plus the real CPU reference export. Full visible graph canonicalization and complete readable effect/ability semantics remain bounded by the supported cases.

The real reference inputs cover 65 learner groups and 72 substeps: Affinity as P0 against Elves, and Rally as P1 against Terror. Five groups have multiple substeps; 18 rows have empty edges and none has empty action references. The latter case, the remaining learner decks and real Ward coverage still need complementary qualification inputs. See [input coverage](E:/mtg-kernel-learned-sideboarding-evidence/engineering-009/reference-input-coverage.json).

This adapter has no JSONL/console entrypoint, incremental BO3 controller, executable mulligan/concession path or human game record. It does not make games against Jack ready. Ordinary duplicate hands must work; unresolved ambiguous graph/semantic cases remain explicit unsupported prompts. Keep human feedback separate from later frozen evaluation.

Remaining unsupported meanings include non-mana activated abilities, spell modes, generic effect options/numbers, non-Ward Boolean choices, spell-copy payment, trigger ordering, dynamic mana amounts, some complex costs and ambiguous duplicate reference graphs. Supporting these and the full human match lifecycle is necessary before games against Jack. The completed decision adapter is an engineering component, not a completed campaign requirement.

## Review and resource boundary

Fresh Fable sessions `f01ee935-a556-4a39-bf94-c6bebd2268d7` (CUDA capture) and `8e56e347-b146-4ec6-886c-414294635f4a` (human boundary) failed weekly HTTP429 with zero source reads or substantive tokens. Root recorded unavailable consultation and continued the expressly assigned reversible work with independent Codex review. No Fable endorsement, quota reset or purchase occurred. Root owns all builds and execution, with four cargo jobs, BelowNormal CPU and E-drive storage. No GPU command, paid allocation, frozen-result modification or playing-strength measurement follows from this preparation.
