[package]
name = "mtg-kernel"
version = "0.0.1"
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]

[build-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
