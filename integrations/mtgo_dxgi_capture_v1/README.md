# MTGO DXGI visible-frame candidate v1

This standalone Windows package captures the foreground MTGO client using DXGI Desktop Duplication. It does not use direct-window capture, accessibility enumeration, process memory, network inspection, client files other than the signed executable identity, hooks, injection, or keyboard input. Its mouse actuator is isolated in `src/actuator.rs` and is unreachable in the current production build because every exact production ratification root remains empty.

The probe is intentionally separate from `mtgo_blackbox_v1`. It contains the Windows `unsafe` boundary needed for Win32, D3D11, DXGI, DWM, process, and Authenticode calls. The offline adapter remains `#![forbid(unsafe_code)]` and cannot call this executable as an input mechanism.

The command-line executable is a thin wrapper over the library capture engine. A successful in-process capture returns `OpaqueMtgoDxgiFrameCandidateV3`. Safe Rust callers cannot construct, clone, format, deserialize, or read pixels from that type. Its public view contains commitments and geometry only. Downstream live wiring must accept the opaque object itself, never a copied commitment or serialized manifest, if it needs proof that the real capture routine ran.

`capture_admitted_mtgo_duel_visible_frame_v1` is the equivalent source boundary for a future measured duel-perception profile. It derives the acting-player duel capture request from the opaque admitted profile, then rechecks the exact executable, signer, DPI, client geometry, monitor-output identity, visible format, canonical pixel geometry, and source-frame hash before returning `OpaqueMtgoAdmittedDuelVisibleFrameV1`. The wrapper binds the profile commitment, profile-admission commitment, and in-process capture commitment. It still exposes no pixels, semantic evidence, model request, action, coordinate, or input conversion. Production duel-profile ratification is currently empty, so this capture route cannot yet be reached by a production caller.

`verify_duel_perception_runtime_v1` and `perceive_admitted_duel_frame_v1` continue that opaque chain through an exact profile-pinned classifier executable. The runtime hashes the classifier binary, classifier-assets manifest, and card-database profile before and after each bounded child-process invocation. Pixels cross only the private stdin protocol. The strict response echoes the exact request commitment and contains the reconstruction audit, observed decision, and complete current visible-control set. Every audit and evidence pixel region is recomputed from the retained BGRA8 bytes, the audit and decision are rebound to the exact checked DXGI artifact and admitted runtime profile, accessibility and manual-annotation evidence are rejected, every legal action belongs to a measured profile family, and the existing full decision validator passes. Every legal action must map one-to-one to an enabled current-frame control. `score_and_select_opaque_admitted_duel_perception_v1` then consumes that source-bound candidate through the admitted-profile model scorer, and `resolve_opaque_profile_bound_duel_control_v1` consumes its profile-bound selection through the existing unique-control resolver while retaining the chosen region privately. All wrappers remain move-only and expose commitments only, with no pixel, observation, selected-semantic, coordinate, input, or event-entry conversion.

`prepare_opaque_competitive_duel_action_plan_v1` now constructs the calibrated visible postcondition and exact League or Challenge match scope directly from that same move-only profile-bound control resolution. It additionally requires the complete semantic gesture blueprint for that exact control and binds its commitment and stage count into the opaque competitive plan. Its internal equality bridge rechecks the decision, model selection, control resolution, perception admission, deployment, selected semantic, control identity, source frame and sequence, serialized capture manifest, raw frame hash, output identity, client geometry, and gesture lineage. The result retains the opaque Windows source, checked gesture blueprint, and checked competitive plan and exposes commitments only. It still has no coordinate, input, purchase, or event-entry conversion. This closes both parallel-plan substitution gaps before the future immediate-recapture actuator boundary.

`bind_opaque_competitive_duel_source_gesture_stage_v1` and `bind_opaque_competitive_duel_continuation_gesture_stage_v1` are dormant target-resolution seams for that plan. The source binder accepts stage zero only. The continuation binder requires a distinct strictly newer opaque perception with the same process and window incarnation, profile, classifier runtime, and unchanged decision payload. Both call the complete target-set validator, rehash each chosen evidence region from retained DXGI pixels, convert only region centers to private desktop points, and commit the primitive and private point list without exposing either. Stage-zero primary controls must exactly equal the model-selected control region and pixels. The continuation seam does not claim an earlier stage was executed, so neither wrapper can reach input; a future sequential actuator must consume stages in order and hold the one-input gate across each visible continuation.

`begin_opaque_competitive_duel_gesture_sequence_v1` and `advance_opaque_competitive_duel_gesture_sequence_v1` add the non-actuating ordered observation chain needed before that actuator. The move-only chain starts at stage zero and can advance only by one declared stage on a distinct newer same-window frame. Each advance requires one same-rectangle pixel change whose before evidence is a prior-stage target or whose after evidence is a next-stage target. Both evidence hashes are recomputed from the retained opaque frames. The chain binds every transition and stage into one commitment, keeps target regions and points private, and explicitly reports that it proves neither input nor action causality. A future executor must place its one-input receipt between adjacent observations and halt the shared gate if this transition check fails.

`review_competitive_duel_gesture_ratification_candidate_from_correspondence_v1` removes the former Pass-only permission mismatch at the review boundary. It binds the exact checked Daybreak correspondence, approved visible account, exactly one League or Challenge mode, and the admitted gesture profile's evaluation, admission, runtime, assets, and complete canonical eleven-family set. The result is non-authorizing review telemetry. The only production constructor requires a separate compile-pinned commitment, which remains `None`, and even its opaque result is permission identity only. It has no event-entry, spending, coordinate, or direct-input conversion. `ratify_competitive_gesture_match_launch_attended_v1` can consume that future permission together with the separately attended exact-game Pass launch only after the owner enters a second OS-random phrase that explicitly extends that exact game to all eleven gestures. `begin_competitive_gesture_game_session_v1` then creates a move-only lineage that binds the profile, permission, original launch, extension receipt, mode, game, and frame lifetime. `bind_competitive_duel_gesture_sequence_session_v1` can consume that exact session together with only the source stage of a matching structurally checked gesture sequence. The join rechecks the exact League or Challenge mode, game number, gameplay authorization, frame lifetime, admitted all-family profile, selected action family, and source frame, then binds both move-only lineages into a coordinate-free commitment. It cannot accept a continuation stage, expose a target, enter an event, spend resources, or recover either consumed lineage. Later boundaries require a fresh frame and the exact pinned target runtime before the shared-gate one-primitive executor can consume it. Live execution remains unreachable because the production roots and runtime admissions are empty.

`verify_duel_gesture_target_runtime_v1` pins the exact reviewed all-family target executable and assets manifest to the admitted gesture profile. `prepare_session_bound_competitive_duel_gesture_source_stage_v1` consumes the joined session lineage and runs only that executable over the retained BGRA8 pixels for exactly the next opaque perception. Its private, environment-cleared, bounded protocol commits the frame, capture, perception result, decision, selected family, gesture plan, stage, primitive, admitted evaluation, runtime, and assets. The response must echo that exact request commitment. The target set then passes the normal plan and decision binder, and every referenced region is rehashed from the retained fresh frame before private click or drag points are created. Runtime artifacts are rehashed before and after invocation. The same MTGO process and window incarnation, perception profile, classifier runtime, visible decision payload, selected-control identity, geometry, and pixels must remain unchanged, and the frame must remain inside the exact game authorization lifetime. The move-only result exposes only commitments and reports `target_runtime_attested_v1` as true. Its own input, event-entry, and spending flags remain false. Only the shared-gate one-primitive executor accepts and consumes it.

`prepare_opaque_competitive_duel_pass_actuation_v1` implements the immediate-recapture boundary for priority Pass only. It acquires a strictly newer opaque DXGI frame, reruns the exact admitted classifier, requires the semantic decision payload to be unchanged, requires the same unique Pass control with identical geometry and pixels, and requires the same process incarnation, window, client geometry, and output. It also recomputes every private calibrated postcondition region from the immediate frame and requires each to remain byte-identical to the plan's source baseline. This prevents an intervening UI change from being credited to a later click. The preparation carries commitments to the complete authorization document, exactly one selected League or Challenge mode, and the exact match gameplay record. The preferred general competitive Pass ratifier consumes the blackbox package's checked-untrusted exact-correspondence review, derives one mode, and requires a compile-pinned commitment to both that review and the priority-Pass-only authorization. The legacy scope-only root remains empty. The legacy compile-pinned match-launch root remains empty and its constructor is no longer public. The exact duel classifier response can include one League or Challenge lifecycle snapshot. The opaque runtime validates that snapshot, rehashes every fact from the same retained pixels as the decision and controls, and retains it privately inside the perception result. A competitive launch or action plan must consume that classifier-bound lifecycle; application code can no longer supply a separate checked snapshot or use the raw control-to-competitive-plan binder. The private duel-launch binder also binds the reviewed event label to a nontrivial region inside the visible match surface and extracts the opponent plus MTGO's visible match and game IDs from the exact stable game-window title. `bind_competitive_event_runtime_to_match_launch_identity_v1` supplies the paid entry authorization and consumes the current main-client event runtime. It requires the exact event, match, game, and entry lineage, the same MTGO process incarnation, and a strictly newer duel-window frame while intentionally allowing the main-client and duel HWND, title, pixels, geometry, and lifecycle-snapshot commitments to differ. The runtime is withheld inside the move-only bridge. `ratify_competitive_event_match_launch_attended_v5` accepts only that bridge. It rejects redirected input and output, displays the event, opponent, visible match and game IDs, account, mode, game number, and abbreviated commitments, generates an OS-random terminal challenge, and returns the same event runtime plus one move-only launch token only after the account owner types the exact mode-and-game phrase. The complete source-bound request is committed into the owner receipt. The token binds the event, match, game, entry record, owner confirmation receipt, launch frame lower bound, and a fixed 512-frame upper bound. It cannot enter an event or spend account resources. `begin_competitive_game_session_v1` consumes both the independent general permission and exact-game launch into one non-Clone session. `bind_prepared_competitive_duel_pass_session_v2` moves that session into one exact prepared Pass. The session is recoverable only from `confirm_pending_competitive_duel_pass_v2` after the newer visible postcondition releases the shared input gate. Each recovery advances a hash chain, action count, and last-confirmed frame; replayed, mismatched, expired, or failed transitions consume the session and cannot authorize another input. Other action families remain blocked until their exact click, double-click, drag, or multi-stage gesture is separately calibrated.

`review_mtgo_authorization_correspondence_v2` is the local, non-authorizing import command for the private Daybreak reply. It accepts a review JSON file, the exact exported correspondence bytes, and a UTF-8 account-alias file containing only the alias with no newline. It verifies the exact hashes and retained restrictions, then prints only the correspondence, account, review, one-mode scope, and proposed production-ratification commitments. It never prints the account alias or message text, retains no private bytes, and reports all live-input, event-entry, and spending flags as false. Example: `cargo run --bin review_mtgo_authorization_correspondence_v2 -- <review.json> <exact-correspondence.bin> <exact-account-alias.txt>`. Its output is review material only. Activating a production root still requires an intentional separately reviewed source change.

`review_competitive_entry_attended_v1` adds the owner-review half of League and Challenge entry wiring without itself adding input authority. It consumes one checked entry-review lifecycle snapshot, derives exactly one mode from the reviewed Daybreak correspondence, and shows the account owner the bounded event label plus exact visible existing-resource type and amount. An OS-random terminal challenge binds the resulting receipt to the correspondence review, one-mode authorization commitment, source frame, event identity, terms, account, label, and exact offline entry-authorization commitment. `bind_opaque_navigation_frame_to_competitive_entry_review_identity_v1` consumes one main-client DXGI candidate, recomputes the complete capture commitment and every lifecycle fact region from retained composed-desktop pixels, and binds the human-reviewed event label to a nontrivial region inside the visible entry-review surface. `review_competitive_entry_attended_v2` remains the manual source-bound path. `bind_classified_navigation_frame_to_competitive_entry_review_identity_v1` instead moves the exact profile, approved-account, classifier runtime, request, response, prediction, lifecycle, and pixel lineage into the same opaque identity. `review_competitive_entry_attended_v3` rejects identities without that retained classifier result and produces a separate move-only classifier-bound review commitment. `bind_classifier_backed_competitive_entry_control_and_deck_dry_run_v2` adds a coordinate-private human-reviewed Join or Enter label, its exact pixel-region hash, explicit visibly-enabled confirmation, and a separate nonoverlapping visible selected-deck label and pixel region. It also binds the externally reviewed exact deck-list manifest, format manifest, and intended policy-deployment commitment. The preferred `review_competitive_entry_attended_v4` shows both the control and selected deck, then requires a second OS-random attended phrase that commits the selected-deck label, region, deck manifest, format manifest, and policy deployment before consuming the complete dry-run identity. Only this v4 lineage can reach the independent exact-entry ratification and executor. All review results remain `CheckedUntrusted`, expose no pixels or coordinates, and return false for live input, event entry, and spending. The control and deck labels are human calibrations, not classifier-accuracy claims. Source binding does not remove the documented transient-occluder race. No result can become actionable until the 18-slice lifecycle profile plus entry-control and selected-deck detectors are evaluated and separately admitted, the occluder limitation is closed, and the independent production exact-entry root is intentionally populated.

`review_competitive_entry_ratification_candidate_v1` recomputes non-authorizing telemetry for one owner-reviewed paid Entry Review from the private correspondence review, approved account, one selected mode, exact visible event and resource terms, source capture, classifier result, selected deck, policy deployment, and coordinate-private enabled control. That telemetry cannot be ratified directly. `bind_confirmed_competitive_open_entry_review_to_entry_review_v1` must consume the move-only visible confirmation for the exact selected Event Browser listing plus a strictly newer control-bound Entry Review. It requires the same account, navigation profile and admission, classifier runtime, MTGO process and window incarnation, geometry, output, mode, event identity, deck-manifest commitment, format, and policy. `review_competitive_entry_ratification_candidate_from_selected_listing_v2` then commits that uninterrupted lineage and exact resource terms. `ratify_competitive_entry_authorization_from_selected_listing_v2` is the only production constructor, and its independent compile-pinned root is currently `None`. `prepare_ratified_competitive_entry_v1` consumes that future move-only authorization, reacquires the admitted main-client frame, reruns the exact profile-pinned classifier, and requires another strictly newer Entry Review frame with the same account, process, window, geometry, output, event, resource terms, visible lifecycle facts, event-label pixels, selected-deck pixels, and visibly enabled control pixels. It retains the fresh capture time, frame identity, and private exact control target. The prepared value exposes no coordinates or general input method. Only the production-disabled exact-entry executor may consume it.

The offline blackbox crate now provides that lifecycle profile's review boundary. `check_untrusted_competitive_navigation_runtime_profile_v1` pins the exact client executable, Daybreak signer, generic main-window title, approved-account alias digest, reviewed visible account-identity region and pixels, DPI, client geometry, display output, classifier binary, and classifier-assets manifest. `check_untrusted_competitive_navigation_source_v1` then recomputes that account-identity region from each complete artifact and discards the raw pixels before the source can enter evaluation. Every profile must cover exactly 18 canonical slices: all nine visible lifecycle phases for League and for Challenge. `check_untrusted_competitive_navigation_corpus_manifest_v1` binds each unique source, expected lifecycle commitment, slice, account profile, annotation protocol, annotator receipt, timestamp, and four explicit visual-review confirmations. Each checked-untrusted prediction binds its declared classifier request and response to the exact profile, source manifest, frame pixels, and lifecycle result, but does not by itself prove that the pinned executable produced the response. `evaluate_untrusted_competitive_navigation_profile_v1` accepts only cases matching that exact checked manifest, requires unique exact-profile, exact-approved-account main-client sources, and measures source-bound predictions against annotated snapshots for all 18 slices. Each slice independently must meet the declared coverage floor; missing slices, duplicate frame pixels, excess abstention, or any non-exact prediction fails the gate. The production evaluation commitment remains `None`. Even a separately ratified profile would grant classifier identity only and remains false for live classification, event entry, spending, and input.

`capture_admitted_mtgo_competitive_navigation_frame_v1` is the corresponding opaque Windows capture boundary. It accepts only that separately admitted 18-slice lifecycle profile and binds a fresh main-client DXGI frame to the exact executable, signer, generic window-title hash, approved-account alias digest, reviewed account-identity pixel region, DPI, client geometry, display output, profile commitment, and profile-admission commitment. The move-only frame exposes commitments only and remains false for lifecycle classification, event entry, spending, and input. Because the production lifecycle evaluation root is empty, application callers cannot currently reach this capture function.

`verify_competitive_navigation_classifier_runtime_v1` and `classify_admitted_mtgo_competitive_navigation_frame_v1` add the next offline-complete runtime seam. The verifier accepts only the classifier executable and asset-manifest hashes already pinned by the admitted 18-slice lifecycle profile. The invocation rechecks both artifacts, sends the retained exact asset-manifest bytes and opaque BGRA8 frame through a bounded private pipe, requires one canonical response bound to the exact request, and rehashes every claimed lifecycle fact against the retained pixels. The move-only result exposes only lifecycle labels and commitments. Its entry-review bridge retains the exact checked source and prediction privately, and the attended v4 dry run binds that complete classifier result to the exact visible event, resource, amount, correspondence, account, frame, owner receipt, coordinate-private enabled control, visible selected-deck calibration, exact deck manifest, format manifest, and intended policy deployment. `bind_classified_navigation_frame_to_lifecycle_control_v1` covers the five non-entry actions: Accept Pairing, Submit Sideboard, Continue After Match, Resume Match, and Close Completed Event. Each source phase must include its exact enabled-control fact, and the binding rehashes that private rectangle against the same retained composed-desktop frame. The resulting move-only value exposes commitments only and cannot input. `prepare_ratified_competitive_entry_v1` uses this same opaque runtime for the immediate pre-actuation recapture and rejects any source, account, runtime, process, window, event, terms, fact, label, control-pixel, selected-deck pixel, deck manifest, format manifest, policy-deployment, frame-order, or timestamp drift. `bind_competitive_entry_postcondition_dry_run_v1` separately pairs the exact reviewed source with a strictly newer classifier-backed entered-waiting frame only when the profile, profile admission, account, classifier runtime, process incarnation, window, geometry, output, mode, and event identity all remain exact and the frame pixels plus lifecycle result have changed. The move-only dry-run pair explicitly makes no Join-causality claim. The separate production-disabled entry executor enforces a two-second capture-age limit at input, rechecks the process, foreground window, DPI, client geometry, and physical hit-test root, emits exactly one left click, and locks the shared process gate. Its bounded confirmation loop releases that gate only after a strictly newer classifier-backed frame shows the same exact League or Challenge event in Entered Waiting for Pairing. It retries only exact unchanged Entry Review candidates; event, terms, account, profile, runtime, process, window, geometry, output, label, control, selected deck, lifecycle, timing, or unexpected-phase drift fails closed and halts later input. Live classification remains false because the capture-time transient-occluder limitation is not yet closed, and the empty production lifecycle-evaluation and exact-entry roots still make the live path unreachable.

`bind_classified_navigation_frame_to_competitive_event_listing_v1` retains one exact Event Browser selection with that same opaque source and navigation-classifier lineage. Before constructing the move-only result, it recomputes the complete frame hash, every lifecycle-fact region, the selected event-label region, and the enabled Open Entry Review control region from the private composed-desktop pixels. It also requires the approved account, exact event, one League or Challenge mode, deck list, deck manifest, format, policy deployment, frame, and lifecycle commitments to match the blackbox selected-listing contract. `classify_checked_untrusted_competitive_event_listing_v1` adds the bounded parser process. Its canonical request commits the exact target, source classifier result, lifecycle, profile, account, runtime, assets, and retained pixels; the strict response can return only one selection for that request, and all visible regions are rehashed before the opaque result is created. A separate binding accepts only the exact admitted two-mode evaluation with matching profile, account, deck, format, and policy commitments. That evaluation's production root is empty, so the evaluated wrapper is unreachable in production. Every layer exposes commitments only and remains false for live classification, Open Entry Review, event entry, spending, and input. An actual reviewed corpus, immediate recapture, separately ratified one-input path, and exact visible Entry Review arrival remain required before this seam may control the client.

`bind_classified_navigation_frame_to_visible_event_record_v1` consumes one classified main-client frame plus a structurally checked League or Challenge event summary. It requires the same lifecycle commitment, frame, event identity, and approved-account digest, rechecks the retained full-frame hash and capture commitment, and recomputes every status, progress, standing, and result region against the private composed-desktop pixels. The move-only output exposes only coordinate-free League record or Challenge round, points, and standing values. Those labels remain checked-untrusted until evaluated on a reviewed corpus, and the output has no input, event-entry, spending, or gameplay conversion.

`classify_checked_untrusted_competitive_event_record_v1` adds the bounded eight-slice exact-field parser exchange. It starts from the opaque admitted main-client frame itself so event status, progress, points, standing, and completion remain independently evaluated rather than inferred from a lifecycle phase label. The request binds the exact frame identity, raw pixel hash and length, capture and profile lineage, approved account, verified runtime, classifier binary, and exact asset-manifest bytes. The profile-pinned process must return one canonical response containing both the lifecycle snapshot and event record for that request. The runtime requires a supported phase, exact frame identity and geometry, validates lifecycle and event-record invariants, and rehashes every declared lifecycle, status, progress, standing, and completion region against the retained same-frame pixels. The move-only result exposes coordinate-free progress and commitments only. No event-record evaluation is ratified in production, and the result remains false for live classification, event entry, spending, gameplay, and input.

`begin_checked_untrusted_competitive_event_monitor_v1` and `advance_checked_untrusted_competitive_event_monitor_v1` retain those move-only results across one exact event. Each advance requires the same profile, runtime, approved account, event identity, and mode, plus a changed capture, frame binding, classifier result, and event record on a strictly newer frame sequence. League and Challenge progress cannot regress or skip more than one match or round. Progress may advance only from pairing-ready into between-match or terminal completion; repeated waiting and pairing observations cannot invent results. Known League totals cannot disappear or change, Challenge round totals cannot change, and visible Challenge points cannot decrease or grow by more than three per completed round. A terminal monitor cannot advance. The hash chain binds every accepted record and observation count. The monitor remains checked-untrusted and exposes no pixels, coordinates, entry, spending, gameplay, or input conversion.

`begin_competitive_event_runtime_after_entry_v1` now joins these pieces into one move-only League or Challenge event coordinator. It can begin only from the exact confirmed entry postcondition and a separately ratified lifecycle authorization for the same account, mode, 18-slice profile, admission, event, correspondence, and owner-reviewed permission commitment. The runtime commitment also retains the exact selected-deck label and pixels, deck manifest, format manifest, and intended policy deployment from the spent entry ratification. Server-observed pairing, game-end, match-end, event-end, and reconnect transitions consume the current opaque classified frame and require the next frame to pass the checked lifecycle state machine under the same classifier lineage. Pairing acceptance, explicitly visible no-change sideboard submission, post-match continuation, reconnect resume, and event close use the shared one-input gate and return the coordinator only after the exact newer visible action postcondition. Model-selected sideboard changes use a separate checked-untrusted parser over the retained sideboarding pixels. It rehashes every lifecycle fact, partition zone, empty drop target, and card row, binds the exact entry deck-list, format, policy, event, match, and game, expands the semantic plan into a deterministic one-card sequence, and accepts each step only after a strictly newer frame visibly matches that one-card inventory transition. Transfers into the mainboard are ordered before transfers out so each intermediate configuration preserves the mainboard minimum and starting sideboard capacity. The resulting ready state still cannot drag or submit, and changed-sideboard plans cannot fall through the generic no-change route. Sideboard automation ratification now also requires the separately admitted four-slice League and Challenge sideboard evaluation for the exact lifecycle profile, account, deck, format, and policy. The coordinator can attach and advance the exact monotonic event-record monitor, and it refuses to close the event until that monitor is terminal. A gameplay checkout withholds the event coordinator and consumes an unbound all-family exact-game session, then hash-binds that session to the event's exact entry ratification, selected-deck label and pixels, deck manifest, format manifest, and policy deployment. A gesture sequence cannot bind to the model-control session before this binding exists, and the selected action plan must carry the same deployment commitment. Returning the game requires every deck and policy commitment plus the underlying entry authorization, correspondence, owner-reviewed permission commitment, mode, account, event, match, game, and frame-lifetime lineage to remain exact. The return records the newest confirmed gameplay-frame sequence, and every later lifecycle frame must be strictly newer than both that gameplay floor and the prior lifecycle frame. The runtime cannot enter another event, spend again, expose pixels or coordinates, or bypass the independent exact-game owner launch. All production roots remain empty, so this is structurally complete but unreachable live wiring.

This is wiring, not a usable live classifier. The production perception-profile, navigation-profile, general competitive permission, lifecycle permission, and exact-entry roots remain empty, the legacy compile-pinned exact-match root remains empty, and no reviewed classifier executable implementing the required runtime protocols has been installed. The attended launch function can create an exact-game token only after consuming the matching paid event runtime into the move-only launch-review bridge, and that token remains insufficient without the separate general-permission ratification and exact opaque gameplay plan. Production-disabled paths now implement an exact authorization-bound all-family game session, one exact competitive entry click followed by bounded visible entered-waiting confirmation, and the full event coordinator described above. The sideboard runtime stops before drag input and before the final Submit Deck control. None of their complete prerequisites can be produced by the current production build.

The exact classifier implementation should use `MtgoDuelPerceptionRequestHeaderV1` and `check_untrusted_duel_perception_request_v1` to verify the canonical header, geometry, all profile and source commitments, byte length, and every BGRA8 pixel before classifying. The shared checker returns metadata and a request commitment only. It does not retain or expose pixels and grants no capture or input authority.

`capture_pinned_current_solitaire_visible_frame_v1` is the first source-pinned live-frame boundary. It constructs the capture request internally and accepts only the inspected MTGO executable hash, exact Daybreak signer certificate identity, local Freeform Solitaire title, 120 DPI, 1550 by 925 client, `DISPLAY2` output identity, SDR BGRA8 geometry, and all-false downstream authority flags. It then rechecks the opaque candidate's bytes and capture commitment before returning `OpaqueMtgoPinnedSolitaireVisibleFrameV1`. The profile commitment is `45f73bf432bbed42e1896c4f02e0670115bb891b69fdc7037c89dc780ac91fac`. A client update, account-title change, window-size change, DPI change, monitor move, GPU/output reorder, rotation, HDR transition, or format drift fails closed until a reviewed source update.

The pinned wrapper proves capture origin plus an exact identity and layout match only. It has no raw-pixel accessor and remains false for semantic evidence, `ObservationV5`, policy scoring, and input. The explicit legacy bridge `into_checked_untrusted_perception_candidate_v1` consumes and downgrades the wrapper to the old authority level. No semantic claim can inherit the profile merely by copying its commitments.

The preferred pregame path now preserves that profile without using the downgrade. Four opaque stages carry the exact profile commitment through prompt measurement, complete seven-card identity measurement, card-aware scoring, and coordinate-private action planning. Each stage consumes the previous unforgeable value, rechecks the compiled profile commitment, retains pixels and templates privately, and has no generic-selection, actuator, coordinate, purchase, or queue-entry conversion. `measure_pinned_current_solitaire_first_main_v1` is the first equivalent gameplay consumer: it carries the same source profile into the reviewed Turn 1 empty-battlefield classifier without exposing the underlying frame. `measure_pinned_current_solitaire_first_main_visible_hand_v1` then combines that opaque state with a checked-untrusted template profile and exposes ordered labels only if all eight fixed art regions match. The first supervised live check matched `Island, Island, Plains, Island, Plains, Plains, Island, Island`. This is a source-provenance and narrow calibration chain, not semantic admission: every evidence, observation, trusted-policy, and live-input flag remains false.

`measure_mtgo_dxgi_mulligan_ladder_candidate_v3` is the first direct perception consumer. It consumes the opaque frame in-process, serializes the probe-owned manifest, rechecks the manifest, PNG, and canonical pixels through `mtgo_blackbox_v1`, then runs the current dark-core binary-ink seven-state London mulligan prompt classifier. The opaque measurement retains the source frame and exposes only the checked-untrusted classification, optional prospective keep size, adapter-local action labels, and commitments. It has no pixel, coordinate, `ObservationV5`, scorer, action-intent, or input conversion.

`measure_mtgo_dxgi_mulligan_visible_hand_candidate_v3` composes that prompt measurement with the checked-untrusted deck template profile and requires all seven displayed cards to match before exposing ordered labels. MTGO displays seven cards at every London mulligan prompt while the prospective keep size descends from seven to one. The combined opaque value retains the frame, pixels, and profile bytes privately and exposes only labels, prompt semantics, and commitments. It grants no semantic, observation, trusted-policy, or input authority.

The original v3 pregame scoring contract binds only the prompt state and actions. It is retained for compatibility but does not contain the visible hand, so it is not sufficient for a meaningful Keep or Mulligan policy. The card-aware v4 contract instead binds the combined opaque measurement, all seven ordered visible card labels, the prospective keep size, the exact ordered Mulligan then Keep actions, both perception-profile commitments, and the same validated native-checkpoint deployment commitment used by gameplay scoring. An external scorer returns finite `f32` bit patterns bound to the exact request. Validation deterministically selects the greatest logit, with the first action winning ties. Stale responses, wrong card or action counts, reordered cards, non-finite values, and unmatched measurements fail closed. `build_card_aware_pregame_action_plan_v4` now carries that exact opaque selection into the existing coordinate-private plan and visible-postcondition path. If that plan later reaches the authorization-gated actuator, its immediate fresh recapture must reproduce all seven exact visible card identities as well as the prompt and selected control pixels. The builder does not itself perform or authorize input, and the production actuator ratification remains empty.

The current mtg-kernel checkpoint lineage has no Keep, Mulligan, or London-bottom action representation, no pregame observation schema, and no pregame training examples. `MtgoNonModelPregameHeuristicV1` is an explicit deterministic wiring stopgap, not a model claim. It scores v4 Keep versus Mulligan from a source-committed card feature table using retained land range, cheap castable spells, and color coverage. It implements the v5 bottoming scorer by choosing the lowest card-retention value and choosing Submit only after six selections. Its historical deployment-envelope fields contain domain-separated `not-a-checkpoint` sentinels that bind the exact heuristic profile. `is_model_backed_v1` and `safe_for_live_input_v1` are always false. Fixed 30 Plains plus 30 Island and 30 Forest plus 30 Island profiles exist only for no-cost Solitaire wiring. A feature profile containing only lands keeps because a redraw cannot improve its land and nonland shape. A trained pregame head remains required for competitive play.

The card-aware bottoming v5 contract carries that model boundary through every London-bottom selection stage. It starts only from the complete seven-card, zero-selected opaque measurement and assigns stable adapter-local object IDs to the initial hand. Each request binds the current opaque capture, current state and identity measurements, exact remaining visible order, confirmed bottomed-card history, complete ordered action set, both perception profiles, and deployment commitment. The zero-selected action set is exactly the seven card selections because the displayed Cancel control is a no-op. Stages one through five add Cancel after the current card selections. A selected card can advance the opaque session only after a strictly newer one-count state transition from the same process, match window, and layout. Those stages reclassify every remaining visible card and require the action-bound order and labels to match. At stage six, the renderer dims the survivor enough to defeat the reviewed art template, so the request explicitly labels that survivor from confirmed action history instead of claiming a fresh visual identity. The final action set is exactly Submit then Cancel. `OpaqueMtgoCardAwareBottomingModelSelectionV5` has no actuator or input conversion, and this tranche does not submit the bottomed cards.

`build_card_aware_bottoming_action_plan_v5` is a later coordinate-private boundary over that exact opaque selection. A card selection binds the current reflowed card-art region, its observed pixel hash, the stable object ID, and the expected next selected and visible-card counts. Submit binds the current Done control and requires a strictly newer first-main measurement from the same process, match window, and layout. Cancel is plannable only at selected counts one through six. It binds the calibrated Cancel-only or Done-plus-Cancel layout and requires a strictly newer complete seven-card, zero-selected identity measurement. Confirmation verifies the same process, match window, layout, perception profile, and ordered card labels, then restores the original stable object IDs, clears the selected-card history, and folds the plan into a new reset-history commitment. The zero-selected no-op Cancel is not an action and cannot be planned. The action plan has no preparation or actuator function, all input safety accessors remain false, and its coordinates cannot be read through the public API.

`build_pregame_action_plan_v3` consumes that opaque selection and binds it to the reviewed 1550 by 925 Solitaire control layout, the exact source capture, and the observed pixels of the selected control region. The target rectangle and point remain private. A planned Mulligan requires the next current ladder prompt on a strictly newer changed frame with the same process, match-window, and output identity. A seven-card Keep can be confirmed by the current Turn 1 first-main profile, which uses binary prompt and Combat-control ink plus exact Turn 1 and empty-battlefield regions. A one-card Keep can be confirmed by `measure_mtgo_dxgi_bottom_six_initial_candidate_v3`, which uses the current binary-control state gate and requires selected count zero. Keeps at prospective sizes two through six remain blocked until their required bottom counts are separately calibrated. Zero-card Mulligan is also rejected until its prompt is calibrated.

`measure_mtgo_dxgi_bottom_six_state_candidate_v3` consumes an opaque DXGI frame and runs the current binary-control state classifier for each reviewed bottom-six selection stage from zero through six selected cards. The returned `OpaqueMtgoDxgiBottomSixStateMeasurementV3` retains the frame, pixels, and private probe geometry. It exposes only immutable commitments, stage counts, Done visibility, and action cardinality. It does not identify cards and all observation, scoring, and input authority remains false.

`measure_mtgo_dxgi_bottom_six_reflow_candidate_v3` consumes two consecutive opaque bottom-six measurements and recognizes exactly one order-preserving visible-card deletion. It rechecks both endpoints with that same current binary-control state classifier instead of falling back to the legacy raw-control gate. The returned `OpaqueMtgoDxgiBottomSixReflowMeasurementV3` exposes only commitments, stage counts, the removed before-frame ordinal, retained-card difference measurements, and the number of passing deletion candidates. It retains both source frames and pixels, rejects ambiguous visual duplicates and skipped stages, does not identify card names, and grants no observation, scoring, or input authority.

`measure_mtgo_dxgi_bottom_six_visible_card_identities_candidate_v3` consumes one opaque bottom-six measurement plus one checked-untrusted deck template profile. It rechecks the exact source pixels in-process with the current binary-control state and full-hand identity classifiers, and returns identities only when every visible ordinal clears the fixed art distance and distinct-name margin. Source pixels and local template pixels remain private. The labels are still caller supplied and unratified, no `CardStableRefV1` binding is created, and all semantic, observation, scoring, and input authority remains false.

The isolated pregame actuator consumes that opaque plan plus an opaque private-match authorization ratification. It immediately recaptures the visible prompt, rechecks the same legal actions, process, window, layout, selected-control pixels, exact account title, and DPI, then verifies the physical point is still owned by the same MTGO process before emitting exactly one left-click. It moves the cursor outside the client afterward and creates an opaque pending receipt. The competitive Pass path uses the same Per-Monitor V2 pointer verifier, the same `SendInput` implementation, and the same process-wide gate. It additionally binds the receipt to the immediate capture, complete before-input region verification, exact competitive mode, and exact authorization. Confirmation now polls admitted duel captures for a bounded 100 to 10000 millisecond window. It retries only while one or more calibrated regions still equal the exact pre-input baseline. Process, window, layout, output, profile, capture-role, ordering, or metadata drift fails immediately. The first candidate on which every calibrated private postcondition region has changed is bound with the exact candidate count and releases the pending receipt. Timeout or any fatal mismatch halts the gate for the process. Pending values expose no handle or coordinates and remain false for next-input, purchase, and queue-entry safety.

Competitive gestures now use the same one-primitive pending-receipt state machine for both one-stage and multi-stage plans. The executor accepts only an opaque session-bound stage produced by the exact profile-pinned target runtime, reserves and halts the process-wide gate, rechecks the foreground MTGO process incarnation, DPI, client geometry, and each visible hit target, then emits exactly one reviewed primitive. Supported primitives use exactly two mouse records for a single click, right click, or drag, and exactly four for a double click. The pending value binds the fresh baseline, current-stage index, target-runtime request and response identities, primitive, input time, record count, and cursor-parking result. A final stage must reach the exact declared visible action postcondition before the all-family game session advances. A non-final stage instead must reach exactly the next declared stage on a newer admitted frame, with a deterministic changed same-rectangle target-related pixel region and the same pinned runtime identity. Any failure after the input attempt leaves the gate halted.

The multi-stage continuation boundary no longer needs caller-supplied target sets or transition probes. Given the exact pending primitive and a strictly newer admitted perception from the same MTGO process, window, layout, profile, decision payload, and authorization lifetime, it promotes the actual immediate pre-input capture to the transition baseline and invokes the exact gesture-profile-pinned target runtime for the declared next stage. It derives a deterministic adjacent-stage transition only from a changed same-rectangle visible region tied to either the prior or next required target, rehashes both retained pixel crops, and joins that observation to the pending input receipt before reopening the gate. The game session is retained unchanged because the semantic action is incomplete. The resulting move-only wrapper cannot emit input. Preparing the next stage consumes it and requires a separate exact next admitted frame, a fresh target-runtime request, unchanged session and authorization identities, and another complete before-input pixel check. Only that separately prepared value can return to the one-primitive executor. The production authorization and runtime-admission roots remain empty, and this implementation did not capture MTGO or send live input.

Production authorization ratification is deliberately `None` for the private-match actuator, legacy scope-only competitive Pass permission, preferred correspondence-review-bound competitive Pass permission, complete eleven-family gesture permission, exact competitive entry authorization, exact sideboard evaluation, exact competitive sideboard automation, and legacy exact competitive match launch. Runtime booleans and arbitrary permission hashes cannot enable any input path. On 2026-08-10, the account owner reported equivalent Daybreak approval for League and Challenge play on the approved main account under the same visible-only and no-cheating condition. The all-family review candidate covers either League or Challenge, one mode at a time, only when it binds the exact account alias, exact private correspondence bytes, admitted gesture profile, and complete canonical family set. If that root is later reviewed and populated, a second attended confirmation must extend one exact already-entered Pass launch before the non-actuating all-family session can exist. Exact event entry has its own independent root binding one owner-reviewed event, visible resource type and amount, source capture, classifier result, enabled control, visible selected-deck label and pixels, external deck-list manifest, format manifest, intended policy-deployment commitment, and attended selected-deck receipt. The immediate recapture must reproduce the same selected-deck pixels and deck, format, and policy commitments before the production-disabled exact-entry executor can consume that fully prepared authority, send one click, and confirm the exact entered-waiting result before releasing the shared gate. Sideboard automation has another independent root bound to the same one-mode lifecycle authority, account, correspondence review, exact deck, format, and policy, plus a separately admitted exact classifier evaluation covering League and Challenge in both changed and unchanged configurations. Its dormant executor immediately recaptures the exact unchanged sideboard state, drags one visible card between the visible zones, requires a strictly newer exact inventory transition before releasing the shared gate, and permits Submit Deck for a changed configuration only through the exact target-ready proof. The generic lifecycle submit route remains no-change only. The attended v5 match path can create a distinct short-lived owner-launch token for one exact already-entered event, match, and game only after the matching paid main-client event runtime is consumed with a strictly newer same-process duel-window perception. The move-only bridge requires the same entry authorization, event, match, and game while explicitly binding both cross-window lifecycle snapshots and frame identities, so application code cannot supply an unrelated entry hash and defer the mismatch until gameplay checkout. Redirected terminal streams are rejected, and the token is valid only from the declared observed frame through 512 later frame sequences. Combined League-plus-Challenge scopes, private or open-play authority, shadow authority, and other prize-event authority are rejected. Approval alone does not send input, all production roots remain empty, and no general queue navigation or purchase executor exists. The prompt shows the exact bounded event label, title-parsed opponent, and visible match and game IDs alongside the account alias, mode, game number, and abbreviated event and match commitments. The event label and selected-deck label are human-reviewed interpretations of committed visible pixel regions, not OCR correctness claims. Other prize-event modes remain disabled unless separately confirmed. No input was sent while implementing or testing this path.

## Admission performed by the probe

Before and after capture, the probe requires:

- Per-Monitor V2 DPI awareness;
- exactly one running `MTGO.exe` process;
- the foreground window to be a visible, responsive, uncloaked, unminimized root window owned by that process;
- the exact expected executable SHA-256;
- successful native `WinVerifyTrust` validation plus the exact pinned signer thumbprint and X.500 subject hash;
- the configured visible title rule;
- DWM composition and `WDA_NONE` display affinity;
- a nonempty physical client rectangle inside its DWM frame;
- no visible top-level window above the target intersecting the client;
- the visible cursor outside the client;
- identical process, signature, focus, geometry, cursor, and z-order snapshots before and after capture.

The DXGI path requires one output to wholly contain the client. It rejects rotation, HDR or noncanonical color space, non-BGRA8 surfaces, pointer-only updates, timeouts, access loss, protected-content masking, and source-texture geometry drift. Canonical output is tightly packed, top-down BGRA8. Staging-row padding is removed before hashing.

Artifacts are written to a new absolute directory outside the repository through a private partial directory and one final rename. The output contains `frame.bgra`, a separately hashed `frame.png` for manual review, and `manifest.json`.

Candidate schema v2 assigns every capture one non-interchangeable visible role:

- `main_client` with role `navigation` and no game format;
- `solitaire_game` with role `acting_player_solitaire` and an exact visible format;
- `duel_game` with role `acting_player_duel` and an exact visible format;
- `spectator_game` with role `spectator` and an exact visible format.

Solitaire titles must visibly identify one participant. Acting-player duel titles must visibly identify one opponent. Spectator titles must visibly identify two participants. The expected format is part of the title rule, so a Standard spectator window cannot be accepted as Freeform or as an acting-player window. The adapter continues to read the earlier main-client-only v1 artifact but rejects any v1 role fields.

## Deliberate nonclaim

Every artifact is marked `checked_untrusted_not_admitted`, and all OCR, semantic-evidence, policy-scoring, and input flags are false. The opaque v3 candidate and mulligan measurement do not change those flags. The pinned wrapper separately establishes one exact capture-origin and identity/layout profile, but does not change any semantic or action flag. These types close the specific caller-forged manifest and safety-boolean path, but Authenticode and frame checks do not prove that a calibration label matches the pixels. Pre/post z-order audits also cannot eliminate a transient occluder that appears and disappears entirely during one frame acquisition. The exact binary-ink prompt templates have only a tiny offline brittleness screen and are not a general accuracy estimate. The dormant actuator requires those exact templates and adds immediate recapture checks, but it does not make perception general or prove that a click caused the later visible state.

The capture API does not focus MTGO. An attended operator must place MTGO in the foreground and park the cursor before capture. The dormant actuator has no keyboard, purchase, queue-entry, event-entry, or focus-stealing path.

## Usage

```powershell
cargo run --release -- `
  --output 'C:\absolute\path\outside\the\repository\new-directory' `
  --expected-exe-sha256 '<lowercase-sha256>' `
  --expected-signer-thumbprint '<lowercase-sha1>' `
  --expected-signer-subject-sha256 '<lowercase-sha256>' `
  --window-mode main_client `
  --timeout-ms 10000
```

An already visible foreground Solitaire window uses:

```powershell
cargo run --release -- `
  --output 'C:\absolute\path\outside\the\repository\new-directory' `
  --expected-exe-sha256 '<lowercase-sha256>' `
  --expected-signer-thumbprint '<lowercase-sha1>' `
  --expected-signer-subject-sha256 '<lowercase-sha256>' `
  --window-mode solitaire_game `
  --expected-game-format Freeform `
  --timeout-ms 10000
```

An already visible foreground acting-player duel window uses the same source checks with `--window-mode duel_game` and the exact visibly named game format. This mode only creates a checked-untrusted artifact. It does not authorize joining a queue, entering an event, scoring a live decision, or sending input.

## Verification

```powershell
cargo test
cargo clippy --all-targets -- -D warnings
```
