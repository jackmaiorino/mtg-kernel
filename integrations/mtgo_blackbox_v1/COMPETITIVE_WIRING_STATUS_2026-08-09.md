# MTGO competitive wiring status, 2026-08-09

Updated 2026-08-11.

## Target

The wiring target is an end-to-end loop capable of joining and completing MTGO League and Challenge matches with the selected mtg-kernel checkpoint, while using only information and controls available to the player. A mode is not wiring-complete until the same runtime can:

1. load exact account, permission, client, calibration, card-database, and model identities;
2. observe every decision in a two-player match without hidden channels;
3. reconstruct an exact `ObservationV5` and complete ordered legal-action vector;
4. score that exact decision with the pinned checkpoint;
5. resolve the selected semantic to one fresh visible control;
6. perform at most one authorized input and visibly confirm its postcondition;
7. handle mulligans, priority, combat, modal choices, targeting, sideboarding, clocks, match transitions, reconnects, and terminal results;
8. stop without input on every unresolved, stale, ambiguous, or low-confidence state.

League and Challenge navigation may share the gameplay loop, but each remains a distinct authorization and lifecycle mode.

## 2026-08-11 scope update

The account owner reports that Daybreak has now given equivalent approval for both League and Challenge play on the approved main account, subject to the same visible-only and no-cheating or hidden-information restriction. This removes the need to treat Challenge play as a later permission request. The exact reply bytes and human review still must pass the existing local correspondence checker before either compile-pinned production root can be populated.

The lifecycle evaluation contract now covers all nine client phases for each mode, 18 slices total, instead of only browser, entry review, and entered-waiting. Pairing-ready, sideboarding, match-complete, event-complete, and reconnect classifications must each include the exact enabled control required for the corresponding action. The Windows boundary consumes that classifier-backed frame, rehashes the private control rectangle against the retained composed-desktop pixels, and returns a move-only, coordinate-private control binding for Accept Pairing, Submit Sideboard, Continue After Match, Close Completed Event, or Resume Match. This tranche detects and binds controls only. It does not authorize or emit lifecycle input.

The future model-facing pregame context now has a separate checked-untrusted structural contract for play-or-draw and the public best-of-three match score. Its three canonical evidence regions are rehashed from the supplied frame, legal game and score combinations are enforced, and the result can bind only to the exact same pregame classification frame and interaction commitment. The admitted eight-state evaluation now records a classifier binary independently from its evaluator, and the Windows path requires that exact binary to equal the already verified runtime executable. It invokes a second bounded protocol over the same retained pixels and consumes both checked classifications into one move-only context. This remains production-blocked and grants no model-scoring or input authority.

The visible UI Automation event-label diagnostic now has a separate capture-bracketed pixel path. One admitted DXGI frame is captured before the read-only exact-text query and another within five seconds afterward. The complete window, process, cursor, z-order, output, role, source-texture, and geometry identities must remain stable, and every private UIA match region must have identical canonical BGRA8 bytes in both exact frames. The output exposes only hashes, counts, control-type IDs, and commitments. It still cannot establish that the UIA name is the text painted in the rectangle, eliminate a transient change-and-revert race, or authorize semantic evidence, scoring, event entry, or input.

The native changed-sideboard blocker is now specified in `COMPETITIVE_SIDEBOARD_MODEL_INTERFACE_2026-08-11.md`. The required kernel surface is a best-of-three match session with an acting-player-visible inter-game history, exact deck partitions, public score and next-game play-or-draw facts, a bounded local one-card transfer and Submit action vocabulary, and a checkpoint-bound sideboard head trained only from terminal match win or loss. The future adapter binder must make that opaque native result the only production source of a target configuration before reusing the existing transfer and Submit Deck chain.

### Competitive pregame interaction binding update

The mode-independent competitive pregame classifier now returns separate, exact current-frame records for all seven visible card identities and for the clickable Keep, Mulligan, London card-selection, and Submit controls. Every card and control region is rehashed against the retained BGRA8 frame. One domain-separated interaction commitment binds the stage, ordered card labels, card-region hashes, control semantics, private rectangles, enabled state, and control-region hashes. The reviewed evaluation requires exact stage plus exact interaction commitments, so stage-only recognition cannot admit the profile.

That interaction commitment now travels through the classified duel frame and exact League or Challenge pregame observation. A deck, format, and gameplay-policy-bound deterministic pregame heuristic can select one visible action from the retained response, producing a move-only action plan that privately retains the selected control rectangle. A second move-only preparation accepts only the immediate next classified capture from the same process, window, profile, runtime, event lifetime, and seven ordered visible card labels. It requires the exact selected control semantic, private rectangle, enabled state, and pixel hash to remain unchanged, then privately resolves the fresh desktop target. The action plan also declares an exact next-state contract for Mulligan, Keep, each London selection, or Submit. An offline dry-run checker requires the selected card to toggle on the same rectangle with changed pixels and rejects wrong or substituted transitions, but it has no input receipt and explicitly claims no causality or next-input safety.

A separate competitive-pregame permission candidate binds the exact reviewed Daybreak correspondence, approved account, exactly one of League or Challenge, and the admitted heuristic profile, algorithm, review, and admission commitments. Its production root is empty. The non-model executor is now crate-private and absent from the public exports, so even a future population of that legacy root cannot make heuristic-selected League or Challenge pregame input reachable from application code. The retained internal implementation still documents capture freshness, the process-wide gate, live target checks, one-click input, exact visible confirmation, and gate release for reuse by a future model-owned path. A new public binder must consume an opaque native pregame selection and its checkpoint provenance first. This implementation performed no live capture, classification, input, event entry, or spending.

### Selected Open Entry Review update

Commit `d98441d` supersedes the older non-actuating selected-listing descriptions below. A separate Open Entry Review-only ratification candidate now binds the exact reviewed correspondence, one League or Challenge mode, approved account, navigation profile, admitted selected-listing evaluation, deck list, canonical deck manifest, format, and policy. Its production root remains empty and independent of both the five-action lifecycle root and paid-entry confirmation.

The move-only preparation consumes one freshly captured evaluated listing and retains the rehashed enabled Open Entry Review control privately. Execution uses the process-wide one-input gate, requires the capture to be at most two seconds old, rechecks the live foreground process, window, DPI, geometry, and target, and emits exactly one left click. Confirmation releases the gate only after a changed, strictly newer post-input frame from the same client incarnation and output visibly reaches Entry Review for the exact selected event. Any mismatch halts the gate. The confirmed arrival cannot confirm paid entry, spend currency, or provide a second input.

The current capture crate lists 153 all-target tests. All 131 non-ignored unit tests, the review-command test, all 19 source-policy tests, all 145 doctests, and strict no-dependency clippy pass. Two opt-in live tests remain ignored. This update performed no live capture, classification, scoring, event entry, spending, or input.

### Selected listing to paid Entry Review lineage update

The paid-entry path now consumes the visibly confirmed selected-listing arrival instead of allowing an unrelated Entry Review capture to begin a separate lineage. `bind_confirmed_competitive_open_entry_review_to_entry_review_v1` requires a strictly newer Entry Review frame from the same approved account, navigation profile and admission, classifier runtime, MTGO process and window incarnation, geometry, display output, League or Challenge mode, exact event identity, deck-manifest commitment, format, and policy deployment. The binding also retains the selected deck-list commitment and exact visible resource terms.

The prior direct paid-entry production constructor has been removed from the public API. Only `ratify_competitive_entry_authorization_from_selected_listing_v2` can create the move-only entry authorization, and its independent compile-time root remains empty. The binding, v2 candidate, and ratified value expose no coordinates and grant no entry, spending, or input authority before ratification. After any future ratification, the existing immediate-recapture, two-second freshness, one-click shared-gate, and strictly newer entered-waiting postcondition checks still apply.

The current capture crate lists 155 all-target tests. All 133 non-ignored unit tests, the review-command test, all 19 source-policy tests, all 147 doctests, and strict no-dependency clippy pass. Two opt-in live tests remain ignored. This update performed no live capture, classification, scoring, event entry, spending, or input.

### Selected-listing Windows OCR parser update

The capture crate now includes a concrete Windows classifier executable for the exact selected-listing child-process protocol. The executable independently rechecks its own binary hash, canonical request, asset-manifest hash, target commitment, complete BGRA8 payload, client geometry, and one exact League or Challenge profile. It emits a selection only when Windows OCR finds the expected target label exactly once inside a reviewed Event Browser card region and a separate nonoverlapping control region byte-matches one of the profile's sorted reviewed enabled Open Entry Review references. Unsupported classifier modes are rejected, and the executable contains no capture, network, process-memory, event-entry, spending, or input operation.

The offline OCR probe found Modern League, Modern Challenge 64, Modern Challenge 96, and Commander Gauntlet League in all three retained preview screenshots without emitting other recognized text. A full framed selected-listing exchange over the retained Modern League preview independently verified the executable, header, asset and pixel hashes, found the unique card-region label, and then rejected at the exact enabled-control reference gate. That rejection is correct because the retained frame visibly says `Please Select a Deck`, and no enabled Open Entry Review reference has been reviewed. This establishes the parser mechanism, not semantic accuracy or runtime admission. The shared production classifier still needs the event-record and sideboard protocol modes, a reviewed two-mode asset corpus, an enabled-control capture with a selected eligible deck, and the existing empty evaluation root to be populated through review.

### Navigation classifier executable update

The same pinned Windows executable now implements the existing competitive-navigation child-process protocol in addition to selected-listing OCR. Its combined asset schema carries ordered League or Challenge lifecycle profiles. Each profile fixes one client layout, lifecycle phase, phase-specific event, match, game, and entry fields, and the complete ordered visible-fact set. Every fact is recognized only when its exact client-relative BGRA region hash matches one of the sorted reviewed references. The binary requires exactly one complete profile match, recomputes every returned fact from the retained frame, and validates the result with the shared lifecycle contract before emitting a request-bound response. Missing enabled controls, pixel drift, invalid phase fields, malformed or unordered assets, zero matches, and multiple matches all reject.

The framed process test proves the exact runtime wire format end to end for a Challenge Pairing Ready fixture containing both the visible pairing state and enabled Accept Pairing control. This removes the missing navigation executable boundary, but it does not populate or validate the required 18-slice corpus. The region matcher remains production-disabled until reviewed League and Challenge examples cover all phases and the existing evaluation root is intentionally populated.

## Current evidence

| Wiring segment | Current evidence | Status | Missing proof or implementation |
| --- | --- | --- | --- |
| Authorization modes | `MtgoAuthorizationScopeV1` separately gates private, open, League, Challenge, and other prize-event input and permanently requires visible-only channels. On 2026-08-11 the account owner reported equivalent Daybreak approval for League and Challenge play on the approved main account under the same visible-only and no-cheating restriction. The exact-correspondence checker recomputes the private reply and account digests, requires a canonical human review of the approved modes and retained safety conditions, and derives only one-mode scopes. A local review command verifies those exact files and emits only hashes plus proposed League and Challenge ratification commitments; it never prints the account alias or message text and cannot authorize input, entry, or spending. The preferred priority-Pass ratifier binds that review to the exact visible account, correspondence, selected mode, and Pass-only gesture scope. A complete-gesture candidate binds the same exact permission identity to the admitted gesture evaluation, profile admission, runtime, assets, and all eleven canonical action families. Its separate production root is empty and it grants no entry or spending authority. If later ratified, a distinct attended upgrade consumes that exact permission plus the prior exact-game Pass launch and requires a second OS-random owner phrase explicitly naming all eleven gestures. The resulting move-only generalized session binds the permission, profile, original owner launch, upgrade receipt, mode, game, and frame lifetime. It can reach the shared-gate executor only after a matching model-selected gesture, fresh opaque frame, and exact profile-pinned target-runtime result are consumed. A separate exact-entry ratification candidate binds the private correspondence, approved account, one mode, owner receipt, exact visible event and resource terms, source capture, classifier result, and enabled control. Its only production constructor requires an independent compile-pinned root. The attended v5 match path consumes the exact paid main-client event runtime together with a strictly newer duel-window perception whose classifier response already contains the same-frame lifecycle interpretation. The runtime rehashes every lifecycle fact from the same retained pixels as the decision and controls, and the move-only bridge requires the same entry authorization, event, match, game, and MTGO process incarnation while committing both cross-window lifecycle snapshots and frame identities before any owner prompt. The prompt displays the visible identities, rejects redirected streams, binds the complete source record into the owner receipt, uses an OS-random exact terminal challenge, and returns the same runtime plus a League or Challenge token with a declared launch-frame lower bound and fixed 512-frame upper bound. It cannot enter an event or spend resources. All production roots remain empty. | Structural exact-correspondence import, all-family permission review, attended exact-game all-family extension, move-only generalized session, independent exact-entry candidate, and paid-event-bound exact-game launch complete; no live exercise | Import and review the exact new correspondence for both modes, populate and review the all-family gesture corpus, then intentionally review any production-root change. Calibrate the event-label region and validate its human interpretation on the first acting-player competitive corpus before a prize-mode rehearsal. Other prize-event modes remain disabled. |
| Client identity | The preview path pins MTGO version and layout. The standalone DXGI candidate additionally pins the executable hash, exact Authenticode signer, unique process, PID, start time, title, DPI, output, and visible window state before and after capture. The source-pinned wrapper fixes the inspected executable, signer, account title, DPI, client size, display output, and canonical frame geometry in one domain-separated profile. | Profile-bound capture origin implemented; semantic identity not admitted | Revalidate and update the source profile after any client, account-title, layout, DPI, monitor, or GPU-output change. |
| Visible capture | The standalone DXGI Desktop Duplication backend acquired real foreground main-client and acting-player Solitaire crops, rejected pointer-only updates, verified current desktop presentations and no protected-content masking, removed staging padding, and emitted separately hashed BGRA8, PNG, and manifest files. V2 makes navigation, acting-player Solitaire, and spectator roles non-interchangeable. The adapter strictly checked the live v2 Freeform artifact as `ActingPlayerSolitaire` and proved PNG-to-BGRA identity. That one exact inspected artifact is source-ratified for offline calibration. The source-pinned live boundary accepts only the inspected executable, Daybreak signer, local Freeform Solitaire title, 120 DPI, 1550 by 925 client, exact `DISPLAY2` output, and canonical SDR BGRA8 geometry, then retains the real frame opaquely. A closed opaque chain now preserves that exact profile through mulligan prompt measurement, seven-card identity measurement, scoring, and coordinate-private planning without an actuator conversion. The acting-player duel path now also retains an admitted DXGI frame through a hash-pinned classifier process and the model scorer without exposing pixels or observations to application code. | Exact source provenance and opaque duel handoff implemented; duel semantics not admitted | Establish the heldout duel semantic accuracy gate and install the exact reviewed classifier executable. Explicitly operationalize the transient-occluder limitation. |
| Perception | Keep, Pass, PlayLand, and Island mana activation have supervised visible transitions with exact pixel-region hashes. The current dark-core mulligan classifier matched all 21 development prompts, rejected four gameplay screens, and matched all seven prompts in a later heldout fixed-deck game. The fixed-deck card-aware mulligan classifier matched all 14 retained prompts and all 98 visible card positions across two games. The current binary-control bottom-six state gate matched every retained stage, two earlier independent initial states, two fixed-deck initial states, and rejected four nearby negatives. The two-card fixed deck identity profile matched 14 of 14 cards across two initial hands and preserved direct identity through five reflows. The final dimmed survivor deliberately fails direct art identity. The current first-main postcondition matched two positive games, including the renderer-drift frame that v1 rejected, and rejected four opening, mulligan, and bottoming screens. Exact visible UIA label matches can now be bound privately to unchanged regions in a five-second pair of exact composed-desktop captures, but this is only a source-binding diagnostic. | Current one-layout pregame classifiers and UIA-to-pixel corroboration connected to opaque capture paths, still checked-untrusted | A much larger heldout labeled corpus and accuracy gates for cards, zones, counters, prompts, phase, priority, stack, combat, timers, game log, enabled controls, and UIA-name-to-painted-label agreement across relevant layouts. These small Solitaire screens are not general accuracy results. Duplicate-art reflows and the final dimmed survivor require action-bound identity history rather than visual ordinal inference. |
| Pregame scoring | The card-aware v4 request binds the exact opaque capture, prompt measurement, all seven ordered visible card labels, prospective keep size, ordered Mulligan and Keep actions, both perception profiles, and one deployment commitment. The v5 bottoming session adds stable initial-hand object IDs, every remaining-card and confirmed-bottomed-card label, complete stage-specific actions, and one-count confirmation across all six selections. Stages one through five require current visible identities. The final dimmed survivor is explicitly sourced from confirmed action history. The deterministic `MtgoNonModelPregameHeuristicV1` implements both scorer traits from a source-committed card feature table, with domain-separated non-checkpoint sentinels and all model-backed and live-input flags false. A v4 card-aware selection can now create the same coordinate-private plan and visible postcondition required by the existing actuator boundary. The Windows producer now adds play-or-draw and the public match score from the exact same retained frame, ties the separately evaluated classifier binary to the verified runtime, and returns one move-only context containing all eight legal public states. | Same-frame visible public context implemented; trained model support absent | Add actual pregame observation and action representations, terminal-outcome training examples, and a checkpoint-bound pregame head before treating pregame choices as model decisions or attempting competitive play. Production context, model, and actuator ratifications remain empty. |
| Duel reconstruction | A fixed ten-group audit identifies the exact `ObservationV5` and legal-action inventory. One retained spectator frame now source-binds the visible participant and turn/phase regions, while the validator permanently rejects spectator pixels as acting-player private knowledge or a legal-action source. The current Solitaire first-main action inventory is explicitly non-transferable: it has eight cards, while the kernel's trained starting player correctly reaches first main with seven. | Incomplete | Capture the acting-player view of an authorized two-player game, then reconstruct opponent state, stable object incarnations, card-database bindings, kernel history context, and the complete ordered legal-action set from that role. |
| Decision validation | `validate_observed_decision_v1` checks exact kernel types, all visible leaf provenance, current-frame evidence, confidence, object bindings, readiness, and action invariants. The opaque duel runtime now additionally requires the one exact live frame identity and geometry, recomputes every root region hash from retained BGRA8 pixels, rejects accessibility and manual-annotation runtime evidence, and rejects action families outside the admitted measured profile. | Implemented for offline records and the production-blocked opaque duel seam | Produce and ratify the first complete reconstructed acting-player duel decision. |
| Model deployment binding | Commit `d33bd4f` binds run, checkpoint manifest, checkpoint payload, train state, model parameters, generation, and scorer contract to each scoring request. `MtgoNativeCheckpointObservationScorerV1` additionally requires every field and a fixed scorer-contract digest to match the immutable loaded handle. The checked-in provisional deployment now pins promoted(2), seed 920012, generation 384. Its loader validated the complete 2.33 GB native Store through generation 512, rewalked the selected generation-384 ancestry, constructed the native inference handle, and matched deployment commitment `1a3288a47a96989b4f4eddd22b0fa2f8db6b083420fd6329e385a7f50641849e`. | Exact provisional package loaded and exercised, no input or match authority | Decide whether this provisional package is the final competitive policy, then score the first complete reconstructed two-player MTGO decision through it. |
| External observation scoring | `NativeCheckpointInferenceV1::score_external_observation_v1` validates the exact full `ObservationV5` plus ordered `ActionSemanticV1` vector, resolves every action reference only against public observation objects, reconstructs the existing Flat V2 scorer view, and calls unchanged native inference. All eleven table families and real fixture-checkpoint outputs match the simulator-owned path exactly. The concrete adapter scorer rebinds the finite result bits to the validated decision request. The exact provisional generation-384 deployment also scored a structurally validated deterministic mock-evidence decision with ordered Pass and PlayLand actions, producing pinned logit bits `[3245259304, 1067419264]` and value bits `1041311617`. A strict one-shot executable now accepts a serialized observed decision and emits only the bound offline selection and intent metadata. The Windows path can now retain the opaque capture through the exact validated decision and only the concrete `MtgoNativeCheckpointObservationScorerV1`. That scorer verifies every loaded Store checkpoint identity field and the fixed scorer contract against the independently supplied deployment before returning commitments, selected index, and score bits. Arbitrary implementations of the external scorer trait cannot enter the opaque Windows path. | Exact selected checkpoint crosses both composed offline and production-blocked opaque seams, no input authority | Replace the kernel-generated mock-evidence probe with the first complete reconstructed two-player MTGO decision. |
| Action selection | Exact response bits are deterministically reduced with the kernel's `total_cmp` argmax and lower-index tie behavior. `score_validated_decision_v1` now composes this with the loaded exact deployment. The real-Store probe selected PlayLand, bound the source decision, and produced only the exact coordinate-free offline intent. | Implemented and exercised offline | Pin the production policy selection rule if competitive deployment should sample instead of use argmax. |
| Visible control resolution | Commit `8b9f4d2` requires a complete current-frame candidate set, exact legal semantics, current pixel evidence, enabled state, confidence, and exactly one selected-semantic match. The bottoming v5 plan additionally binds each current reflowed card, both calibrated Cancel layouts, or final Done control to its exact source pixels and required visible postcondition while keeping coordinates private. Cancel confirmation restores the original stable object inventory only after a newer complete zero-selected identity measurement. The opaque duel classifier protocol now returns the reconstruction audit, complete current control set, and decision, recomputes every audit and evidence region from the retained pixels, and carries the exact source-bound candidate through admitted-profile scoring and profile-bound control resolution. The Windows path now constructs the calibrated postcondition and League or Challenge match scope from that same move-only resolution and rejects any lineage mismatch. A new gesture grammar represents all eleven action families as one-input stages, including direct activation, fresh-frame context-menu continuation, calibrated drag, exact semantic object sets, trigger order, and submit. A complete target-set contract resolves each stage only to one current visible primary control, menu choice, exact bound object, play area, order slot, or submit control. The opaque competitive plan binds the checked gesture commitment and stage count to the same control lineage, and the Windows source and continuation binders rehash chosen target pixels and retain private center points. A move-only observation chain now enforces exact stage order and requires a same-rectangle pixel change tied to an adjacent stage before it advances. | Structural resolver, complete semantic gesture and target grammar, dormant opaque fresh-frame stage binding, noncausal ordered transition chain, closed competitive-plan lineage, and fixed-layout bottoming retention implemented | Build and review the per-family corpus, run the gesture-target evaluator, then add a sequential actuator that places one input receipt between adjacent observed stages under the shared gate. |
| One-input discipline | `MtgoMockActuatorV1` permits one pending action and halts if the next validated decision does not show the declared visible leaf transition. A separate attended helper emitted the first live Keep click while rechecking exact process, signer, focus, DPI, geometry, hit-test root, and cursor position. The opaque competitive path prepares priority Pass against a strictly newer admitted capture and classifier result only when the decision payload, unique control identity, control geometry and pixels, process incarnation, window, client geometry, output, and every private postcondition baseline region are unchanged. Its exact League or Challenge mode commitment and match-gameplay commitment can join only matching general-permission and exact-owner-launch ratifications. Those two ratifications enter one non-Clone Pass session. Each prepared Pass consumes that session, and only a successfully confirmed newer visible postcondition returns an advanced session with a new chain commitment, action count, and last-confirmed frame. A replayed, mismatched, expired, or failed transition cannot recover the session. The attended launch additionally rejects Pass preparation before its launch frame or after its 512-frame lifetime. The all-family path now applies that discipline to all eleven gesture families. Each stage is prepared from a fresh opaque frame by the exact profile-pinned target runtime, with referenced pixels rehashed and private targets retained. The shared-gate executor emits exactly one click, right click, double click, or drag primitive and retains the pending receipt. A final stage returns the advanced session only after the exact newer visible action postcondition. A non-final stage must show the exact adjacent stage and target-related pixel change, then requires another fresh runtime-backed preparation before the next primitive. Replay, stage skips, stale frames, lineage drift, ambiguous targets, and failed postconditions halt the gate. | Full-family target-attested sequential execution and confirmation implemented behind empty production roots; no live exercise | Populate and pass the all-family gesture corpus, install the reviewed target runtime, then fault-inject every family in a no-stakes attended rehearsal. |
| Postcondition monitoring | Mock predicates are decision-bound and require a newer frame and changed decision commitment. The live Keep pair now has a byte-checked newer acting-player frame with required prompt, player-counts, game-log, phase-bar, and hand changes. | One live calibration transition, not runtime monitoring | Trusted live-frame ingestion, semantic recognizers, and action-specific visible predicates for every supported semantic. |
| Match lifecycle | A visible state machine covers pairing, game launch, sideboarding, match result, reconnect, and return to the event. It requires phase-specific current-frame facts, fresh frames, and event and match identity continuity. A separate selected-listing contract now binds one Event Browser frame to the approved account, exact League or Challenge event identity and visible label, deck list, deck manifest, format, policy deployment, and enabled Entry Review control. Its coordinate-free intent can be confirmed only by a strictly newer Entry Review frame naming the same event, and it grants no input, entry-confirmation, or spending authority. The Windows seam consumes the opaque classified Event Browser frame, recomputes the full frame, every lifecycle fact, the event label, and the enabled Open Entry Review control from retained pixels, and returns only source-bound commitments. Its bounded parser process binds the exact target, source classification, lifecycle, profile, account, runtime, assets, and pixels into a strict request, accepts one exact response, and rehashes every declared region. A separate two-slice reviewed-corpus evaluator requires both League and Challenge selections, overall and per-mode coverage, and exact event, source-frame, label-region, and enabled-control semantics. The Windows evaluation binder requires those exact profile, account, deck, format, and policy commitments. Its production root is empty, so the parser remains unratified and non-actionable. The same classifier executable now answers the navigation protocol from reviewed exact-region profiles and returns only a unique full lifecycle match that passes the shared phase contract. Its third protocol links one reviewed event-record profile to that exact lifecycle profile, requires a unique full-region match, and returns the validated lifecycle and League or Challenge record under one request commitment. A separate event-record contract binds waiting, pairing-ready, between-match, and completed-event summaries to the exact lifecycle frame, event identity, and approved account. League and Challenge progress schemas are distinct. The Windows seam rehashes every status, progress, standing, and result region against the retained opaque pixels before exposing coordinate-free record values. An eight-slice reviewed-corpus contract separately measures coverage and exact accuracy for lifecycle, event identity, mode, status, match record, schedule, Challenge points and standing, and completion. The bounded parser starts directly from the admitted main-client frame, binds exact pixels, capture lineage, profile, runtime, binary, and assets into one request, receives lifecycle and event record together, and rehashes every declared fact before returning coordinate-free data. A move-only event monitor then requires exact profile, runtime, account, event, and mode continuity, changed strictly newer frames, allowed status movement, monotonic one-match or one-round progress, and terminal lockout while chaining every accepted record. Entry review separately binds a nonoverlapping visible selected-deck label and pixel region, the exact external deck-list and format manifests, the intended policy-deployment commitment, an attended review receipt, unchanged immediate-recapture pixels, and event-runtime retention. Gameplay checkout binds that exact entry, deck, and policy to the model-control session, and selected action plans must carry the same policy deployment. | Structural lifecycle, exact selected-event pre-entry contract, bounded current-pixel listing, navigation, and event-record parser modes, same-frame pixel-bound event record, monotonic event monitor, selected-deck and policy entry binding, and non-authorizing evaluation gates implemented; no reviewed lifecycle or event-record corpus exists and production evaluation admission is empty | Populate and evaluate the selected-listing, 18-slice navigation, and eight-slice event-record corpora, then capture and review the entry-control and selected-deck corpora, execute the pinned parsers and monitor, and verify actual match acceptance, sideboarding, timers, reconnect recovery, and safe exit. |
| League and Challenge lifecycle | Separate modes share a visible transition contract from event browser through entry review, queue, pairing, matches, sideboarding, reconnect, and event completion. The lifecycle evaluation requires all 18 mode-and-phase slices. The selected-listing contract requires exactly one authorized mode and preserves the exact event, account, deck, format, and policy identity from the Event Browser to matching Entry Review, but remains checked-untrusted and non-actuating. Its two-slice evaluator requires both League and Challenge, exact semantic localization, and overall plus per-mode coverage, with an empty production root. Pairing, sideboard, post-match, reconnect, and event-completion sources each carry the exact enabled-control fact for the available action. Each of the five non-entry controls now uses the shared process gate, one exact click, and a newer classifier-backed visible postcondition before another input can occur. A move-only event coordinator starts only from the confirmed exact entry, chains action and observed lifecycle transitions, withholds itself through an exact-game gameplay lease, attaches the monotonic event-record monitor, and refuses event close without a terminal record. Entry and lifecycle authority must share the exact reviewed correspondence, permission review, account, and one-mode commitment. Before attended match launch, the current main-client runtime consumes with a strictly newer duel-window perception whose exact classifier response already contains its lifecycle interpretation. The private runtime rehashes every lifecycle region from the same pixels as the decision and controls. The move-only bridge checks the exact entry authorization, event, match, game, and MTGO process incarnation while committing both cross-window snapshots and frames. The same retained lifecycle is later consumed into the competitive action plan, and the raw caller-supplied lifecycle and control-to-plan binders are no longer public. The owner-confirmed launch returns that same runtime, so application code cannot mint an independent launch and defer substitution detection until checkout. Gameplay checkout and return preserve that same permission lineage and the underlying entry authorization, preventing another review or entry with the same visible event identity from substituting its game session. The runtime also retains the last confirmed gameplay-frame sequence and rejects every later lifecycle candidate that is not strictly newer, preventing a post-game rollback to a frame captured before the model's last action. Changed sideboarding has an exact deck-list-bound pixel parser, deterministic one-card transfer coordinator, separately ratified immediate-recapture drag executor, and proof-specific changed-deck Submit Deck bridge. A separate commitment-only evaluator requires League and Challenge examples for both changed and unchanged configurations, measures overall and worst-slice coverage and exact full-snapshot accuracy, and binds its ratification to the exact profile, approved account, deck, format, and policy. Sideboard automation ratification now requires that separately admitted evaluation. Every drag rechecks the foreground process, geometry, and source and destination ownership, then requires the exact next visible inventory before releasing the shared gate. Generic lifecycle preparation rejects every sideboard submission without retained exact target-ready provenance. The changed-deck route must advance to the exact next game number, and unchanged submission remains unavailable until a future proof-specific native model binder exists. Entry confirmation remains independently bound to exact account resources and an owner receipt. All production roots remain empty. | Full move-only structural event wiring for exact listing selection, entry, pairing, paid-event-bound attended launch, gameplay lease, changed-sideboard drag and submission, reconnect, results, continuation, and close; listing and sideboard accuracy gates defined; production-disabled | Populate and review selected-listing examples, all 18 lifecycle slices, the event-record corpus, and the four-slice sideboard corpus, close transient-occluder handling, ratify exact artifacts, and complete no-stakes attended rehearsals before any prize entry. |
| Verification | The isolated adapter lists 359 all-target tests, with the opt-in real-Store external-scoring test ignored. The separate capture crate lists 208 all-target tests, with 206 passing and two opt-in live tests ignored. Both all-target and doctest suites pass, and both crates pass strict no-dependency clippy. Seven selected-listing contract tests cover exact League and Challenge arrival, source-frame and mode substitution, deck, format, policy, event-label identity, control readiness and geometry, exact account and one-mode scope, stale or wrong arrival, authorization drift, and unknown fields. Ten listing-evaluation tests cover both canonical mode slices, exact private admission, empty production ratification, abstention, wrong-but-valid event selection, trace-ID and confidence invariance, source substitution, incomplete visual review, weak accuracy thresholds, slice omission, and unknown fields. Six Windows library unit tests, eleven executable-classifier unit tests, four complete framed navigation, event-record, sideboard, and duel child-process tests, one OCR unit test, and the relevant source-policy test cover exact current-pixel binding, canonical bounded parser requests and responses, executable, asset, and card-database self-binding, exact OCR card-region localization, rejection of unreviewed controls, unique complete lifecycle, event-record, sideboard, and duel profile matching, invalid phase fields, semantic mismatch, region drift, ambiguity, pixel, asset, target and deck substitution, full-frame and declared-fact rehashing, the required admitted-evaluation seams, exact approved account, opaque retention, and absence of input authority. The sideboard evaluator tests cover all four canonical slices, exact admission, empty production ratification, abstention, wrong-but-valid configuration, source substitution, incomplete visual review, weak thresholds, slice omission, and unknown fields. The capture source-policy tests require that admitted evaluation plus the independent empty sideboard-automation root, immediate recapture, one-drag shared-gate receipt, exact newer inventory confirmation, and proof-specific changed-sideboard submission while requiring the event driver to stop at sideboard resolution and forbidding generic unchanged submission without opaque native model provenance. Retained real artifacts confirm all 98 fixed-deck mulligan card positions, and the reflow v2 path matches five consecutive unique deletions, rejects a skipped stage, rejects duplicate-art ambiguity, and fails closed on direct visual identity for the final dimmed survivor. The v5 request preserves that survivor only as action-confirmed history. The Cancel corpus covers the visible zero-stage no-op and exact resets from selected counts one through six. | Green for the adapter's current offline and production-disabled library and integration scope | Import the exact approval and account identity, then collect live selected-listing, shadow-match, gesture, lifecycle, event-record, and sideboard corpora, run fault injection and timer and reconnect tests, and complete a private-match end-to-end rehearsal before any prize mode. |

The paid-event duel launch now also requires a separately admitted competitive duel-lifecycle evaluation. Its reviewed corpus must contain unique Match-in-Progress examples for both League and Challenge, satisfy overall and per-mode coverage floors, and match every lifecycle semantic exactly. The gate is bound to the same duel perception profile and admission used for the source frame, and both evaluation commitments are carried through the cross-window launch identity. Its production ratification root remains empty, so structural pixel binding alone cannot make a competitive launch actionable.

The event coordinator now exposes one exact non-authorizing driver directive for every post-entry phase. It distinguishes gameplay launch from post-game observation using the retained returned-gameplay frame floor, routes only the phase-appropriate lifecycle action, requires terminal event-record confirmation before close, and rejects pre-entry, inconsistent lease, or inconsistent event-monitor state. Event-record monitor creation now requires a separately admitted eight-slice League and Challenge parser evaluation for the exact profile and approved account, with its evaluation and admission commitments retained in the monitor chain. Monitors also retain the exact MTGO process-incarnation commitment across records and must match the coordinator's current main-client frame, preventing records from an unreviewed parser, restarted client, or second client from entering the active event. At Event Complete the driver distinguishes initial terminal-monitor creation from advancement of an already attached monitor. Every checked Sideboarding frame now stops at explicit sideboard resolution, even when the exact pixels show an unchanged configuration. Generic lifecycle preparation rejects `SubmitSideboard` without retained exact target-ready provenance. MTG-kernel has no native sideboard decision interface, and the prior event-level planner that accepted an externally supplied target configuration remains test-only. Application code therefore cannot reach changed-sideboard transfer or submission, and cannot submit an unchanged deck, until a concrete checkpoint-bound selection source and proof-specific binder exist.

The pinned classifier executable now also implements the bounded sideboard protocol expected by that structural runtime. It requires the byte-identical prior Sideboarding lifecycle and one exact reviewed profile linked to the selected navigation profile and validated deck manifest. Both board zones, both empty drop targets, and every canonically ordered card row are matched and rehashed against the same frame. The returned deck, format, policy, event, match, game, and lifecycle identities pass the shared sideboard validator. An actual framed process test runs navigation followed by sideboard classification on the same fixture. This closes parser mechanics only. The four-slice reviewed corpus, production evaluation admission, and native terminal-outcome-trained sideboard head remain absent, so neither changed nor unchanged sideboard submission is model-ready or live-enabled.

The same pinned executable now implements the acting-player duel protocol already invoked by the opaque capture runtime. It self-binds the executable, classifier assets, card-database profile, canonical request, exact source-manifest hash, and retained BGRA8 bytes. One reviewed reference profile must uniquely match every decision-evidence, reconstruction-audit, visible-control, and optional competitive-lifecycle region. The classifier patches only fresh frame identity, reruns the shared decision, reconstruction, lifecycle, and control contracts, and returns one checked-untrusted response. A complete framed process test verifies the request commitment, source-manifest propagation, decision and lifecycle frame binding, and fail-closed pixel drift. This closes the executable protocol gap, not the semantic-accuracy gate. The acting-player duel corpus and production profile root remain empty.

Current verification supersedes the earlier table counts: the isolated adapter lists 359 all-target tests and the capture crate lists 208. Of the capture tests, 206 pass and two opt-in live tests remain ignored. All 171 capture doctests pass, and both crates pass strict clippy. The dedicated duel-lifecycle cases cover exact two-mode success, empty production ratification, a missing mode, abstention, semantic drift, and duplicate-frame rejection. Event-record tests now also cover exact eight-slice admission and the empty production ratification. The event-driver test covers every post-entry route, initial gameplay versus returned-game observation, terminal-record gating, and invalid lease or identity state. Eleven executable-classifier tests and four complete framed child-process tests now cover selected-listing OCR plus unique exact-region lifecycle, event-record, sideboard, and acting-player duel classification, including lifecycle equality across the chained navigation protocols and source-manifest binding for fresh duel frames.

## Shortest critical path

1. Use the explicit deterministic heuristic only to exercise pregame wiring. Add trained mulligan and London-bottoming representations and checkpoint heads as a separate model-readiness requirement.
2. Populate and evaluate the exact acting-player duel reference corpus for the now-implemented profile-bound classifier protocol. The capture, classifier, validator, and scorer handoff are wired without a raw-pixel callback, but the production profile root remains empty.
3. Run a continuously captured two-player no-stakes match to close observation, action-set, object-incarnation, and history reconstruction once a non-human opponent path is authorized and available.
4. Implement measured perception and current-frame control resolution for the decision families observed in that corpus.
5. Install the reviewed classifier and exact authorization artifacts, then exercise the dormant priority-Pass click and postcondition gate in shadow mode first and private-match mode second. Add only measured gestures for other action families.
6. Exercise the implemented move-only event coordinator in repeated no-stakes rehearsals, including pairing, gameplay lease return, model-owned unchanged and changed sideboard submission, post-match continuation, reconnect resume, terminal result capture, and event close. Reject any unresolved state or unsafe input.
7. Populate selected-listing examples, the 18-slice League and Challenge lifecycle corpus, and the defined event-record and four-slice sideboard corpora. Evaluate the pinned parsers and enabled-control facts, then exercise the production-disabled open-review, entry, drag, Submit Deck, and visible postcondition paths in no-stakes attended rehearsals. Enable neither mode's independent production roots until the corpora, transient-occluder handling, and rehearsals are reviewed.
8. Enable a prize mode only after the exact permission record, account, model, client profile, deck, and entry constraints are reviewed and pinned.

The current branch does not claim live gameplay readiness. It now has a working visible-pixel capture candidate, a source-pinned opaque live capture-origin and identity/layout wrapper, current pregame classifiers wired into the opaque in-process path at checked-untrusted authority, card-aware scoring boundaries for both mulligan and all London-bottom selections, an explicitly non-model deterministic pregame wiring heuristic restricted to offline planning, a coordinate-private non-actuating bottoming plan with visible transition checks, an exact external in-game checkpoint scorer, a fully Store-validated provisional generation-384 deployment with no input or match authority, an exact selected-event Event Browser to Entry Review contract with opaque current-pixel binding, a bounded selected-listing parser and empty two-mode evaluation root, an 18-slice League/Challenge lifecycle evaluation gate with an empty production root, shared-gate enabled-control execution and confirmation for every non-entry lifecycle action, an eight-slice event-record evaluation gate, a four-slice exact sideboard evaluation gate, bounded exact-field parsers, move-only monotonic event monitoring with no production ratification, and a classifier-plus-control-bound attended exact-entry review. The selected-event contract remains non-actuating until its reviewed corpus is populated and admitted, then immediate recapture, separately ratified one-input execution, and an exact visible arrival postcondition are implemented. The production-disabled exact-entry transaction performs the fresh matching Entry Review recapture, enforces capture age and live target checks, emits one click under the shared gate, and releases that gate only after bounded exact-event Entered Waiting for Pairing confirmation. The move-only event coordinator then binds that entry to one exact mode, account, event, profile, lifecycle authority, gameplay lease, event-record chain, and every visible lifecycle transition through terminal close. Its separate sideboard path preserves the same account, mode, deck, format, policy, and admitted four-slice evaluation identities through immediate recapture, one-card drag receipts, exact newer inventory confirmation, and changed-deck Submit Deck into the next game. The opaque duel path retains the frame through pixel-bound reconstruction, source-bound scoring, profile-bound current-control resolution, full-family semantic gesture planning, same-window adjacent-stage transition chaining, calibrated postcondition planning, exact League or Challenge match scoping, immediate before-input region verification, shared-gate gesture primitives, and exact newer-frame postcondition confirmation. The largest shared blockers remain the real two-player classifier and corpus, native terminal-outcome-trained pregame and sideboard heads, exact private approval import, reviewed production ratifications, populated selected-listing, lifecycle, event-record, and sideboard corpora with version-specific control detection, closure of transient-occluder handling, a no-stakes end-to-end rehearsal, and live calibrated execution evidence for every gesture family.
