[package]
name = "mtgo-blackbox-v1"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
mtg-kernel = { path = "../../mtg-kernel" }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["float_roundtrip"] }
sha2 = "0.10"
